package game;
// global static variables
public class GlobSettings {
    // define game space size for example 100 for 100x100 game space
    static final int SPACE_SIZE = 100;

    // static final int OBS_INITIAL_SIZE = 10;
    // define obstacle initial size for example 10 for 10x10
    // define obstacle size change
    static  final int OBSTACLE_SIZE_CHANGE = 10;
}
